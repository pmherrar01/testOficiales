PORT=4000
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=baloncesto
DB_PASSWORD=baloncesto
# No lo cambies salvo que también cambies el nombre fijo en database/schema.sql
# y en docker-compose.yml / start-db.sh (ver comentarios allí).
DB_NAME=baloncesto_examen
CORS_ORIGIN=http://localhost:5173
# Genera el tuyo con: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=cambia_esto_por_un_valor_aleatorio_largo
