DB_ROOT_PASSWORD=rootpass
# El nombre de la base de datos NO se configura aquí: está fijo a
# "baloncesto_examen" en docker-compose.yml / start-db.sh y en database/schema.sql.
DB_USER=baloncesto
DB_PASSWORD=baloncesto
DB_PORT=3307
